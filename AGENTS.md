# Agent instructions

See [`CLAUDE.md`](./CLAUDE.md) — that file is the source of truth for how
agents should work in this repo (sync/commit protocol, project layout,
resume-vs-new logic, progress logging). It's named `CLAUDE.md` because
that's the file Claude Code auto-loads, but the instructions apply to any
agent working in this repo.
