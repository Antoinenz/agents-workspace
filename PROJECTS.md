# Project registry

One row per project under `projects/`. Keep this current — see
`CLAUDE.md` §6. Newest / most relevant edits don't need special
ordering; just keep every project represented.

| Project | Status | Last updated | Summary |
|---|---|---|---|
| _(none yet)_ | | | |

Status values: `active`, `blocked`, `done`, `paused`.
